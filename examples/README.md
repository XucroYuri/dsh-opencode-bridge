# Examples

## demo.sh

End-to-end demo that starts the OpenCode server, starts the proxy, and calls the proxy with curl.

```bash
./examples/demo.sh
```

Requires:

- DSH installed
- `dsh-opencode-bridge` installed into a DSH profile
- OpenCode CLI available
